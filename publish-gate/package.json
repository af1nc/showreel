{
  "name": "publish-gate",
  "version": "1.0.0",
  "description": "A pre-publish validation gate for machine-generated editorial copy: every checkable claim is recomputed from the data, with asymmetric tolerance, a fail-open design, recorded overrides, and a backtest plus negative control.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "demo": "tsx src/demo.ts",
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "validation",
    "guardrails",
    "data-quality",
    "audit-trail",
    "fail-open",
    "backtest",
    "negative-control"
  ],
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
