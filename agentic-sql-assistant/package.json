{
  "name": "agentic-sql-assistant",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A production-shaped agentic RAG-over-SQL assistant: a raw LLM tool-calling loop with a read-only SQL guard, per-round parallel tool calls, soft-deadline reasoning degradation, and volatile-vs-stable answer caching. No LangChain, no API key, no cloud.",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "demo": "tsx src/demo.ts",
    "test": "node --import tsx --test src/sqlGuard.test.ts",
    "check": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
